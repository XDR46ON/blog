{% extends 'base.html.twig' %}

{% block body %}
    <div class="container py-5">
        {{ form_start(form) }}
            {{ form_row(form.name) }}
            <button class="btn btn-primary">
                {{ button_label|default('Save') }}
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}
